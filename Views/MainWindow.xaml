<Window x:Class="CMusic.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CMusic"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Load" Click="MenuItemLoad_Click"/>
                <Separator/>
                <MenuItem Header="_Exit" Click="MenuItemExit_Click" />
            </MenuItem>
            <MenuItem Header="_CMusic">
                <MenuItem Header="_Version" Click="MenuItemVersion_Click"/>
            </MenuItem>
        </Menu>
        <Grid DockPanel.Dock="Bottom" Height="25" VerticalAlignment="Bottom">
            <Label x:Name="titleSongLabel"/>
        </Grid>
        <Grid DockPanel.Dock="Bottom" Height="25" VerticalAlignment="Bottom">
            <Slider></Slider>
        </Grid>

    </DockPanel>
</Window>
